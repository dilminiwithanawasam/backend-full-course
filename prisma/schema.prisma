// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

// Notice: NO datasource block here anymore!
// It's moved to prisma.config.ts

model User {
  id    Int    @id @default(autoincrement())
  name  String
  email String @unique
}